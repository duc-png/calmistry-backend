server:
  port: 8080
  servlet:
    context-path: /calmistry

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://metro.proxy.rlwy.net:24473/calmistry}
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:vNuRxEsasQqlbOZUsraXWnRGoQIqXkVN}
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

#outbound:
#  identity:
#    client-id: "<clientId>"
#    client-secret: "<clientSecret>"
#    redirect-uri: "http://localhost:3000/authenticate"

  application:
    name: spring-ai-demo
  ai:
    openai:
      api-key: ${SPRING_AI_KEY:}
      base-url: ${SPRING_AI_BASE_URL:https://ccs.qatthcv.com/}
      chat:
        options:
          model: gpt-5.2

# JWT Configuration
jwt:
  signerKey: ${JWT_SIGNER_KEY:}
  valid-duration: 3600 # in second
  refreshable-duration: 36000 # in seconds

# OpenAI Configuration
openai:
  api-key: ${OPENAI_API_KEY:}
  model: gpt-5.2



# Gemini AI Configuration
gemini:
  api-key: ${GEMINI_API_KEY:}
  model: gemini-1.5-flash

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:}
  api-key: ${CLOUDINARY_API_KEY:}
  api-secret: ${CLOUDINARY_API_SECRET:}
